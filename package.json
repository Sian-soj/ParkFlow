{
    "name": "parkflow-unified",
    "version": "1.0.0",
    "description": "Unified ParkFlow Management system",
    "scripts": {
        "install:all": "npm install --prefix frontend && npm install --prefix ParkFlow-Admin/frontend",
        "start:resident": "npm run dev --prefix frontend",
        "start:admin": "npm run dev --prefix ParkFlow-Admin/frontend",
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix ParkFlow-Admin/frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
